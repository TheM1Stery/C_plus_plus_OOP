#include "LinkedList.h"
#include <iostream>
#include <cmath>

int main()
{
    // LinkedList<int> list;
    // list.push_back(10);
    // for(auto& item : list)
    // {
    //     std::cout << item << ' '
    // }
    double a;
    std::cin >> a;
    std::cout << ceil(a / 3); // 1.2 -> 2
}